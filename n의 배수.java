class Solution {
    public int solution(int num, int n) {
        int answer = 0;
        if (num % n == 0) {
            return 1; //Java에서는 int 리턴 메서드가 모든 경우에 return 값을 줘야 함
        } else {
            return answer;
        }
    }
}
