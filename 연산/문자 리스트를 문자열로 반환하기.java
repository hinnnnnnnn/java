class Solution {
    public String solution(String[] arr) {
        String answer = "";
        
        for (String a : arr) { //⭐️for-each문⭐️
              answer += a;
        }

        return answer;
    }
}
